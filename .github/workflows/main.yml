# GitHub Action for generating a contribution graph with a snake eating your contributions.
name: Generate Snake

# Bu Action her 6 saatte bir otomatik olarak çalışacaktır.
on:
  schedule:
      # every 6 hours
    - cron: "0 */6 * * *"

# Bu komut, Actions sekmesinden manuel olarak çalıştırmanıza olanak tanır.
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:

    # Deponuza erişmek için çekme (checkout) işlemi.
      - uses: actions/checkout@v2

    # Yılanı oluşturan Action.
      - uses: Platane/snk@master
        id: snake-gif
        with:
          # BURASI DÜZELTİLDİ: Senin GitHub kullanıcı adın.
          github_user_name: karadagabdi 
          # Dosyaların 'dist' klasörüne ve 'output' dalına (branch) gitmesini sağlar.
          gif_out_path: dist/github-contribution-grid-snake.gif
          svg_out_path: dist/github-contribution-grid-snake.svg

     # Hata ayıklama için git durumunu gösterir.
      - run: git status

      # Değişiklikleri ana dala (branch) iter.
      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
          # Gerekli izinleri sağlayan token.
          github_token: ${{ secrets.GITHUB_TOKEN }}
          # BURASI DÜZELTİLDİ: Ana dal (branch) adını 'main' olarak ayarladık.
          branch: main 
          force: true

      - uses: crazy-max/ghaction-github-pages@v2.1.3
        with:
          # Yukarıda bahsedilen çıktı dalı.
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
